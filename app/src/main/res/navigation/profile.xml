<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/profile"
    app:startDestination="@id/profile2">

    <fragment
        android:id="@+id/profile2"
        android:name="com.example.novinshop_project.feature.profile.Profile"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" >
        <action
            android:id="@+id/action_profile2_to_favourit"
            app:destination="@id/favourit" />
    </fragment>
    <fragment
        android:id="@+id/favourit"
        android:name="com.example.novinshop_project.feature.profile.favourite.Favourit"
        android:label="fragment_favourit"
        tools:layout="@layout/fragment_favourit" />
</navigation>